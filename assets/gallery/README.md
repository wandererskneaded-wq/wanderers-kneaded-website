# Gallery Image Names

Upload final image files into this folder using these simple names:

- `t1.jpg`, `t2.jpg` for street food units, vans and trailer setups.
- `f1.jpg` to `f7.jpg` for food and pizza close-ups.

The website already points to these filenames. Keep the filenames the same when replacing images so the page updates without code changes.
